# Rust Public API Reference

FormulaTracer 0.1.1 / generated from the public headers and native source.

Internal items are excluded. Experimental entries are listed but are not stability promises.

| Symbol | Stability | Signature | Ownership / failure |
|---|---|---|---|
| `Interval` | PUBLIC_EXPERIMENTAL | `pub struct Interval` | Result/error and fail-closed behavior are defined by the native core. |
| `VerificationStatus` | PUBLIC_EXPERIMENTAL | `pub enum VerificationStatus` | Result/error and fail-closed behavior are defined by the native core. |
| `ErrorEvidence` | PUBLIC_EXPERIMENTAL | `pub struct ErrorEvidence` | Result/error and fail-closed behavior are defined by the native core. |
| `compose_absolute_errors` | PUBLIC_EXPERIMENTAL | `pub fn compose_absolute_errors` | Result/error and fail-closed behavior are defined by the native core. |
| `InfiniteProcessStatus` | PUBLIC_EXPERIMENTAL | `pub enum InfiniteProcessStatus` | Result/error and fail-closed behavior are defined by the native core. |
| `InfiniteProcessEvidence` | PUBLIC_EXPERIMENTAL | `pub struct InfiniteProcessEvidence` | Result/error and fail-closed behavior are defined by the native core. |
| `require_truncation` | PUBLIC_EXPERIMENTAL | `pub fn require_truncation` | Result/error and fail-closed behavior are defined by the native core. |
| `Formula` | PUBLIC_STABLE | `pub struct Formula` | Result/error and fail-closed behavior are defined by the native core. |
| `SemanticObject` | PUBLIC_STABLE | `pub struct SemanticObject` | Result/error and fail-closed behavior are defined by the native core. |
| `VerificationResult` | PUBLIC_STABLE | `pub struct VerificationResult` | Result/error and fail-closed behavior are defined by the native core. |
| `approximation_family_operation` | PUBLIC_EXPERIMENTAL | `pub fn approximation_family_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `approximation_proof_operation` | PUBLIC_EXPERIMENTAL | `pub fn approximation_proof_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `Signedness` | PUBLIC_EXPERIMENTAL | `pub enum Signedness` | Result/error and fail-closed behavior are defined by the native core. |
| `Overflow` | PUBLIC_EXPERIMENTAL | `pub enum Overflow` | Result/error and fail-closed behavior are defined by the native core. |
| `BitRepresentation` | PUBLIC_EXPERIMENTAL | `pub struct BitRepresentation` | Result/error and fail-closed behavior are defined by the native core. |
| `bit_and` | PUBLIC_EXPERIMENTAL | `pub fn bit_and` | Result/error and fail-closed behavior are defined by the native core. |
| `bit_or` | PUBLIC_EXPERIMENTAL | `pub fn bit_or` | Result/error and fail-closed behavior are defined by the native core. |
| `bit_xor` | PUBLIC_EXPERIMENTAL | `pub fn bit_xor` | Result/error and fail-closed behavior are defined by the native core. |
| `bit_not` | PUBLIC_EXPERIMENTAL | `pub fn bit_not` | Result/error and fail-closed behavior are defined by the native core. |
| `bitvector_operation` | PUBLIC_EXPERIMENTAL | `pub fn bitvector_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `project_audit_bundle` | PUBLIC_EXPERIMENTAL | `pub fn project_audit_bundle` | Result/error and fail-closed behavior are defined by the native core. |
| `AuditBundle` | PUBLIC_EXPERIMENTAL | `pub struct AuditBundle` | Result/error and fail-closed behavior are defined by the native core. |
| `CanonicalPolicy` | PUBLIC_EXPERIMENTAL | `pub struct CanonicalPolicy` | Result/error and fail-closed behavior are defined by the native core. |
| `canonicalize` | PUBLIC_EXPERIMENTAL | `pub fn canonicalize` | Result/error and fail-closed behavior are defined by the native core. |
| `canonical_json` | PUBLIC_EXPERIMENTAL | `pub fn canonical_json` | Result/error and fail-closed behavior are defined by the native core. |
| `semantic_hash` | PUBLIC_EXPERIMENTAL | `pub fn semantic_hash` | Result/error and fail-closed behavior are defined by the native core. |
| `document_semantic_hash` | PUBLIC_EXPERIMENTAL | `pub fn document_semantic_hash` | Result/error and fail-closed behavior are defined by the native core. |
| `semantic_equal` | PUBLIC_EXPERIMENTAL | `pub fn semantic_equal` | Result/error and fail-closed behavior are defined by the native core. |
| `TruthStatus` | PUBLIC_EXPERIMENTAL | `pub enum TruthStatus` | Result/error and fail-closed behavior are defined by the native core. |
| `Fact` | PUBLIC_EXPERIMENTAL | `pub struct Fact` | Result/error and fail-closed behavior are defined by the native core. |
| `FactEngine` | PUBLIC_EXPERIMENTAL | `pub struct FactEngine` | Result/error and fail-closed behavior are defined by the native core. |
| `OriginSet` | PUBLIC_EXPERIMENTAL | `pub struct OriginSet` | Result/error and fail-closed behavior are defined by the native core. |
| `NativeProvenanceNode` | PUBLIC_EXPERIMENTAL | `pub struct NativeProvenanceNode` | Result/error and fail-closed behavior are defined by the native core. |
| `NativeProvenanceEdge` | PUBLIC_EXPERIMENTAL | `pub struct NativeProvenanceEdge` | Result/error and fail-closed behavior are defined by the native core. |
| `NativeProvenanceGraph` | PUBLIC_EXPERIMENTAL | `pub struct NativeProvenanceGraph` | Result/error and fail-closed behavior are defined by the native core. |
| `origin_set_operation` | PUBLIC_EXPERIMENTAL | `pub fn origin_set_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `resolve_configuration` | PUBLIC_EXPERIMENTAL | `pub fn resolve_configuration` | Result/error and fail-closed behavior are defined by the native core. |
| `compare_dataset_schemas` | PUBLIC_EXPERIMENTAL | `pub fn compare_dataset_schemas` | Result/error and fail-closed behavior are defined by the native core. |
| `build_data_lineage` | PUBLIC_EXPERIMENTAL | `pub fn build_data_lineage` | Result/error and fail-closed behavior are defined by the native core. |
| `assemble_provenance` | PUBLIC_EXPERIMENTAL | `pub fn assemble_provenance` | Result/error and fail-closed behavior are defined by the native core. |
| `debug_project` | PUBLIC_EXPERIMENTAL | `pub fn debug_project` | Result/error and fail-closed behavior are defined by the native core. |
| `select_minimal_reproducer` | PUBLIC_EXPERIMENTAL | `pub fn select_minimal_reproducer` | Result/error and fail-closed behavior are defined by the native core. |
| `ENode` | PUBLIC_EXPERIMENTAL | `pub struct ENode` | Result/error and fail-closed behavior are defined by the native core. |
| `RewriteTrace` | PUBLIC_EXPERIMENTAL | `pub struct RewriteTrace` | Result/error and fail-closed behavior are defined by the native core. |
| `ExactEGraph` | PUBLIC_EXPERIMENTAL | `pub struct ExactEGraph` | Result/error and fail-closed behavior are defined by the native core. |
| `assemble_project_verification` | PUBLIC_EXPERIMENTAL | `pub fn assemble_project_verification` | Result/error and fail-closed behavior are defined by the native core. |
| `FormulaTracerError` | PUBLIC_EXPERIMENTAL | `pub enum FormulaTracerError` | Result/error and fail-closed behavior are defined by the native core. |
| `build_error_analysis` | PUBLIC_EXPERIMENTAL | `pub fn build_error_analysis` | Result/error and fail-closed behavior are defined by the native core. |
| `ErrorBound` | PUBLIC_EXPERIMENTAL | `pub struct ErrorBound` | Result/error and fail-closed behavior are defined by the native core. |
| `ErrorComponent` | PUBLIC_EXPERIMENTAL | `pub struct ErrorComponent` | Result/error and fail-closed behavior are defined by the native core. |
| `ProofObligation` | PUBLIC_EXPERIMENTAL | `pub struct ProofObligation` | Result/error and fail-closed behavior are defined by the native core. |
| `FunctionSensitivityContract` | PUBLIC_EXPERIMENTAL | `pub struct FunctionSensitivityContract` | Result/error and fail-closed behavior are defined by the native core. |
| `ErrorComposition` | PUBLIC_EXPERIMENTAL | `pub struct ErrorComposition` | Result/error and fail-closed behavior are defined by the native core. |
| `CompositionResult` | PUBLIC_EXPERIMENTAL | `pub struct CompositionResult` | Result/error and fail-closed behavior are defined by the native core. |
| `PropagationNode` | PUBLIC_EXPERIMENTAL | `pub struct PropagationNode` | Result/error and fail-closed behavior are defined by the native core. |
| `GraphPropagationResult` | PUBLIC_EXPERIMENTAL | `pub struct GraphPropagationResult` | Result/error and fail-closed behavior are defined by the native core. |
| `GraphPropagationRequest` | PUBLIC_EXPERIMENTAL | `pub struct GraphPropagationRequest` | Result/error and fail-closed behavior are defined by the native core. |
| `CompositionRequest` | PUBLIC_EXPERIMENTAL | `pub struct CompositionRequest` | Result/error and fail-closed behavior are defined by the native core. |
| `compose_error_components` | PUBLIC_EXPERIMENTAL | `pub fn compose_error_components` | Result/error and fail-closed behavior are defined by the native core. |
| `evaluate_error_budget` | PUBLIC_EXPERIMENTAL | `pub fn evaluate_error_budget` | Result/error and fail-closed behavior are defined by the native core. |
| `propagate_expression_graph` | PUBLIC_EXPERIMENTAL | `pub fn propagate_expression_graph` | Result/error and fail-closed behavior are defined by the native core. |
| `MathematicalFunction` | PUBLIC_STABLE | `pub struct MathematicalFunction` | Result/error and fail-closed behavior are defined by the native core. |
| `NumericDomain` | PUBLIC_EXPERIMENTAL | `pub enum NumericDomain` | Result/error and fail-closed behavior are defined by the native core. |
| `AlgebraicStructure` | PUBLIC_EXPERIMENTAL | `pub enum AlgebraicStructure` | Result/error and fail-closed behavior are defined by the native core. |
| `SourceSpan` | PUBLIC_EXPERIMENTAL | `pub struct SourceSpan` | Result/error and fail-closed behavior are defined by the native core. |
| `SourceOrigin` | PUBLIC_EXPERIMENTAL | `pub struct SourceOrigin` | Result/error and fail-closed behavior are defined by the native core. |
| `SemanticDocument` | PUBLIC_EXPERIMENTAL | `pub struct SemanticDocument` | Result/error and fail-closed behavior are defined by the native core. |
| `Shape` | PUBLIC_EXPERIMENTAL | `pub struct Shape` | Result/error and fail-closed behavior are defined by the native core. |
| `FunctionType` | PUBLIC_EXPERIMENTAL | `pub struct FunctionType` | Result/error and fail-closed behavior are defined by the native core. |
| `execute_kernel` | PUBLIC_EXPERIMENTAL | `pub fn execute_kernel` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_core_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_core_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_equality_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_equality_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_expression_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_expression_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_ieee754_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_ieee754_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_interval_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_interval_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_knowledge_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_knowledge_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_math_semantics_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_math_semantics_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_numeric_types_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_numeric_types_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_probability_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_probability_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_synthesis_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_synthesis_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `legacy_transformations_operation` | PUBLIC_EXPERIMENTAL | `pub fn legacy_transformations_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `logic_operation` | PUBLIC_EXPERIMENTAL | `pub fn logic_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `KnowledgeRule` | PUBLIC_EXPERIMENTAL | `pub struct KnowledgeRule` | Result/error and fail-closed behavior are defined by the native core. |
| `KnowledgePack` | PUBLIC_EXPERIMENTAL | `pub struct KnowledgePack` | Result/error and fail-closed behavior are defined by the native core. |
| `ProviderContract` | PUBLIC_EXPERIMENTAL | `pub struct ProviderContract` | Result/error and fail-closed behavior are defined by the native core. |
| `ProviderPack` | PUBLIC_EXPERIMENTAL | `pub struct ProviderPack` | Result/error and fail-closed behavior are defined by the native core. |
| `ProviderMatch` | PUBLIC_EXPERIMENTAL | `pub struct ProviderMatch` | Result/error and fail-closed behavior are defined by the native core. |
| `ScientificDefinition` | PUBLIC_EXPERIMENTAL | `pub struct ScientificDefinition` | Result/error and fail-closed behavior are defined by the native core. |
| `FormalizationLevel` | PUBLIC_EXPERIMENTAL | `pub enum FormalizationLevel` | Result/error and fail-closed behavior are defined by the native core. |
| `ScientificTheorem` | PUBLIC_EXPERIMENTAL | `pub struct ScientificTheorem` | Result/error and fail-closed behavior are defined by the native core. |
| `ScientificRealization` | PUBLIC_EXPERIMENTAL | `pub struct ScientificRealization` | Result/error and fail-closed behavior are defined by the native core. |
| `ScientificFoundationPack` | PUBLIC_EXPERIMENTAL | `pub struct ScientificFoundationPack` | Result/error and fail-closed behavior are defined by the native core. |
| `scientific_foundation_operation` | PUBLIC_EXPERIMENTAL | `pub fn scientific_foundation_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `parallel_operation` | PUBLIC_EXPERIMENTAL | `pub fn parallel_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `plan_generation_native` | PUBLIC_EXPERIMENTAL | `pub fn plan_generation_native` | Result/error and fail-closed behavior are defined by the native core. |
| `ProvenanceNode` | PUBLIC_EXPERIMENTAL | `pub struct ProvenanceNode` | Result/error and fail-closed behavior are defined by the native core. |
| `ProvenanceEdge` | PUBLIC_EXPERIMENTAL | `pub struct ProvenanceEdge` | Result/error and fail-closed behavior are defined by the native core. |
| `ProvenanceGraph` | PUBLIC_EXPERIMENTAL | `pub struct ProvenanceGraph` | Result/error and fail-closed behavior are defined by the native core. |
| `CacheKey` | PUBLIC_EXPERIMENTAL | `pub struct CacheKey` | Result/error and fail-closed behavior are defined by the native core. |
| `IntegrityEnvelope` | PUBLIC_EXPERIMENTAL | `pub struct IntegrityEnvelope` | Result/error and fail-closed behavior are defined by the native core. |
| `LocalizationLevel` | PUBLIC_EXPERIMENTAL | `pub enum LocalizationLevel` | Result/error and fail-closed behavior are defined by the native core. |
| `DebugLocalization` | PUBLIC_EXPERIMENTAL | `pub struct DebugLocalization` | Result/error and fail-closed behavior are defined by the native core. |
| `localize` | PUBLIC_EXPERIMENTAL | `pub fn localize` | Result/error and fail-closed behavior are defined by the native core. |
| `ReconstructionStatus` | PUBLIC_EXPERIMENTAL | `pub enum ReconstructionStatus` | Result/error and fail-closed behavior are defined by the native core. |
| `ReconstructionRelation` | PUBLIC_EXPERIMENTAL | `pub struct ReconstructionRelation` | Result/error and fail-closed behavior are defined by the native core. |
| `ReconstructionSafety` | PUBLIC_EXPERIMENTAL | `pub struct ReconstructionSafety` | Result/error and fail-closed behavior are defined by the native core. |
| `TemporaryAssignment` | PUBLIC_EXPERIMENTAL | `pub struct TemporaryAssignment` | Result/error and fail-closed behavior are defined by the native core. |
| `ProviderProjection` | PUBLIC_EXPERIMENTAL | `pub struct ProviderProjection` | Result/error and fail-closed behavior are defined by the native core. |
| `ReconstructionRequest` | PUBLIC_EXPERIMENTAL | `pub struct ReconstructionRequest` | Result/error and fail-closed behavior are defined by the native core. |
| `ReconstructionResult` | PUBLIC_EXPERIMENTAL | `pub struct ReconstructionResult` | Result/error and fail-closed behavior are defined by the native core. |
| `reconstruct` | PUBLIC_EXPERIMENTAL | `pub fn reconstruct` | Result/error and fail-closed behavior are defined by the native core. |
| `RelationKind` | PUBLIC_EXPERIMENTAL | `pub enum RelationKind` | Result/error and fail-closed behavior are defined by the native core. |
| `RelationEdge` | PUBLIC_EXPERIMENTAL | `pub struct RelationEdge` | Result/error and fail-closed behavior are defined by the native core. |
| `RelationGraph` | PUBLIC_EXPERIMENTAL | `pub struct RelationGraph` | Result/error and fail-closed behavior are defined by the native core. |
| `to_tex` | PUBLIC_EXPERIMENTAL | `pub fn to_tex` | Result/error and fail-closed behavior are defined by the native core. |
| `representation_operation` | PUBLIC_EXPERIMENTAL | `pub fn representation_operation` | Result/error and fail-closed behavior are defined by the native core. |
| `StructuralFacts` | PUBLIC_EXPERIMENTAL | `pub struct StructuralFacts` | Result/error and fail-closed behavior are defined by the native core. |
| `OperandPermutation` | PUBLIC_EXPERIMENTAL | `pub struct OperandPermutation` | Result/error and fail-closed behavior are defined by the native core. |
| `IsomorphismWitness` | PUBLIC_EXPERIMENTAL | `pub struct IsomorphismWitness` | Result/error and fail-closed behavior are defined by the native core. |
| `QuotientNormalizationResult` | PUBLIC_EXPERIMENTAL | `pub struct QuotientNormalizationResult` | Result/error and fail-closed behavior are defined by the native core. |
| `StructuralIsomorphismResult` | PUBLIC_EXPERIMENTAL | `pub struct StructuralIsomorphismResult` | Result/error and fail-closed behavior are defined by the native core. |
| `quotient_normalize` | PUBLIC_EXPERIMENTAL | `pub fn quotient_normalize` | Result/error and fail-closed behavior are defined by the native core. |
| `structural_isomorphism` | PUBLIC_EXPERIMENTAL | `pub fn structural_isomorphism` | Result/error and fail-closed behavior are defined by the native core. |
| `parse_tex` | PUBLIC_EXPERIMENTAL | `pub fn parse_tex` | Result/error and fail-closed behavior are defined by the native core. |
| `TypedSubstitution` | PUBLIC_EXPERIMENTAL | `pub struct TypedSubstitution` | Result/error and fail-closed behavior are defined by the native core. |
| `UnificationResult` | PUBLIC_EXPERIMENTAL | `pub struct UnificationResult` | Result/error and fail-closed behavior are defined by the native core. |
| `typed_unify` | PUBLIC_EXPERIMENTAL | `pub fn typed_unify` | Result/error and fail-closed behavior are defined by the native core. |
| `substitute` | PUBLIC_EXPERIMENTAL | `pub fn substitute` | Result/error and fail-closed behavior are defined by the native core. |
| `unit_operation` | PUBLIC_EXPERIMENTAL | `pub fn unit_operation` | Result/error and fail-closed behavior are defined by the native core. |
