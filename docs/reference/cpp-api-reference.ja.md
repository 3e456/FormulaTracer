# C++ RAII API Reference（日本語）

FormulaTracer 0.1.1 / generated from the public headers and native source.

internal項目は除外します。Experimental項目は一覧化しますが安定性保証ではありません。

| Symbol | Stability | Signature | Ownership / failure |
|---|---|---|---|
| `Context` | PUBLIC_STABLE | `class Context` | Result/error and fail-closed behavior are defined by the native core. |
| `MathematicalFunction` | PUBLIC_STABLE | `class MathematicalFunction` | Result/error and fail-closed behavior are defined by the native core. |
| `SemanticObject` | PUBLIC_STABLE | `class SemanticObject` | Result/error and fail-closed behavior are defined by the native core. |
| `Result` | PUBLIC_STABLE | `class Result` | Result/error and fail-closed behavior are defined by the native core. |
| `Formula` | PUBLIC_STABLE | `class Formula` | Result/error and fail-closed behavior are defined by the native core. |
